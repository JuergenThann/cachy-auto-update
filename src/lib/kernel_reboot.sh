#!/bin/bash

# kernel_reboot.sh: Check if there's a pending kernel update requiring a reboot to be applied
# https://github.com/Antiz96/arch-update
# SPDX-License-Identifier: GPL-3.0-or-later

# deliberately deleted content. CachyOS will display required restart anyways.
# will also use kernel-modules-hook to avoid system from breaking without restart.
